{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capability for the main window: core window and event access only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
