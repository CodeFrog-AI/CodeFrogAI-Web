{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core window and event access, plus opening the native folder picker. No filesystem, shell, or network permissions.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
